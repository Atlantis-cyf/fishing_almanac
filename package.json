{
  "name": "fishing_almanac",
  "private": true,
  "description": "Flutter web + Express BFF. Vercel: Root Directory empty → vercel.json (see scripts/vercel-*.sh). Backend-only project: Root Directory backend → backend/vercel.json.",
  "engines": {
    "node": ">=18"
  },
  "workspaces": ["backend"],
  "scripts": {
    "build": "sh scripts/edgeone-pages-build.sh"
  }
}

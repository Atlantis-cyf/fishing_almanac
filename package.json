{
  "name": "fishing_almanac",
  "private": true,
  "description": "Flutter web + Express BFF. Vercel repo-root: Root ./ → vercel.json + scripts/vercel-*.sh; second project same root, set FLUTTER_WEB_TARGET=admin for admin web. Backend-only: Root backend → backend/vercel.json.",
  "engines": {
    "node": ">=18"
  },
  "workspaces": ["backend"],
  "scripts": {
    "build": "sh scripts/edgeone-pages-build.sh"
  }
}
